/// Identifiers used to name each route in the application.
class RouteNames {
  /// Name of the splash route.
  static const splash = '/splash';

  /// Name of the root route.
  static const root = '/';

  /// Name of the database route.
  static const database = '/database';

  /// Name of the database visualizer route.
  static const databaseVisualizer = '/database-visualizer';

  /// Name of the SQL basic suggestion settings route.
  static const sqlBasicSuggestionSettings =
      '/settings/sql-basic-suggestion-settings';

  /// Name of the SQL advanced suggestion settings route.
  static const sqlAdvancedSuggestionSettings =
      '/settings/sql-advanced-suggestion-settings';

  /// Name of the SQL suggestion settings route.
  static const sqlSuggestionSettings = '/settings/sql-suggestion-settings';

  /// Name of the workspace layout settings route.
  static const workspaceLayoutSettings = '/settings/workspace-layout-settings';
}
