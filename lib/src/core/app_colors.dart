import 'package:flutter/material.dart';

/// Centralized color palette used across the app.
class AppColors {
  AppColors._();

  /// Accent color used for aqua-blue highlights.
  static const aquaBlue = Color(0xFF00BCD4);
}
